let hello = 'Hello';

let world = 'world';

let epicPhrase = hello + ' ' + world + '!';
console.log(epicPhrase);

// template literals
let epicPharse2 = `${hello} ${world}!`;// comillas francesas
console.log(epicPharse2);

// Milti.line string
let lorem = 'esto es un string \n' + 'esto es otra linea';

let lorem2 = `Esta es una frase epica
  la continuación de esa frase epica.
`;

console.log(lorem);
console.log(lorem2);